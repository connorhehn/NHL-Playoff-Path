{
  "name": "nhl-playoff-path",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "destroy": "cdk destroy",
    "diff": "cdk diff",
    "frontend:dev": "npm --prefix frontend install && npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend install && npm --prefix frontend run build"
  },
  "devDependencies": {
    "aws-cdk": "^2.200.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.0",
    "@types/node": "^22.0.0",
    "source-map-support": "^0.5.21"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.200.0",
    "constructs": "^10.0.0"
  }
}
